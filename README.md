# Client Server Mail Service

The project implements, through the use of graphical interfaces built with JavaFX, an e-mail service.
The system was built through the use of the Model View Controller (MVC) architecture and it is divided into:
- Server
  It handles multiple client requests simultaneously
- Clients
  Allows users to manage their mailbox by sending, removing and viewing their messages.
